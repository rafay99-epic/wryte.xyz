# Project-local bun config — overrides ~/.bunfig.toml for this repo only.

[install]
# No `minimumReleaseAge` gate. It was blocking the very thing it was added to
# allow: upstream security patches ship faster than any release-age window,
# and holding them back leaves `bun audit` advertising known-vulnerable
# versions. Security bumps land as soon as they publish.
